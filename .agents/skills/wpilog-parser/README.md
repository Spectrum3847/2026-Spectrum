# wpilog-parser

Agent skill for the [wpilog-parser](https://github.com/jonahsnider/wpilog-parser/) project.

The vendored skill exposes the parser's Rust binary for reading WPILOG files in agent workflows. The upstream project version is pinned by the vendored build artifacts; see the upstream repository for its license terms.
