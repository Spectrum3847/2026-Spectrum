# wpilog-parser

Agent skill for the [wpilog-parser](https://github.com/jonahsnider/wpilog-parser/) project.

The vendored skill wraps the TypeScript/JavaScript `wpilog-parser` library for reading WPILOG files in agent workflows. The upstream project version is tracked by the skill's manifest; see the upstream repository for its license terms.
